@model IEnumerable<EProject.Models.EF.StoneQltyMst>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Name quality if stone
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StoneQlty)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StoneQlty_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StoneQlty_ID })
        </td>
    </tr>
}

</table>
