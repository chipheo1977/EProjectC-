@model IEnumerable<EProject.Models.EF.GoldKrtMst>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Carat
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Gold_Crt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GoldType_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GoldType_ID })
        </td>
    </tr>
}

</table>
