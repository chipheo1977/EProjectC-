@model IEnumerable<EProject.Models.EF.DimQltyMst>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DimQly)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DimQly)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DimQly_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DimQly_ID })
        </td>
    </tr>
}

</table>
